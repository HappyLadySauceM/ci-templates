apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The ARC Helm values are consumed by the deploy repository's ApplicationSet.
# Keeping a kustomization marker makes the snapshot promotion atomic while
# allowing the source tree to remain the single writer for ARC configuration.
resources: []
